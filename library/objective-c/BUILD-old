load("@build_bazel_rules_apple//apple:ios.bzl", "ios_application", "ios_framework", "ios_static_framework")

alias(
    name = "envoy",
    actual = "@envoy//source/exe:envoy",
)

# ios_static_framework(
#     name = "foo",
#     hdrs = glob(["envoy/**/*.h"]), # This doesn't work because the headers aren't public
#     minimum_os_version = "10.0",
#     deps = ["@envoy//source/exe:envoy_main_common_lib"],
# )

objc_library(
    name = "appmain",
    srcs = [
        "AppDelegate.h",
        "AppDelegate.mm",
        "main.mm",
    ],
    copts = ["-std=c++14"],
    sdk_dylibs = [
        "resolv.9",
        "c++",
    ],
    deps = ["@envoy//source/exe:envoy_main_common_lib"],
)

ios_application(
    name = "app",
    bundle_id = "com.foo.bar",
    families = ["iphone"],
    infoplists = ["Info.plist"],
    minimum_os_version = "10.0",
    deps = ["appmain"],
)
