syntax = "proto3";

package io.envoyproxy.envoymobile;

message Request {
  string str = 1;
}
message Response {
  string str = 1;
}

service EnvoyMobileTest {
  rpc stream(stream Request) returns (stream Response) {
  }

  rpc unary(Request) returns (Response) {
  }
}