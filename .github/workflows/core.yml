name: core

on: [push]

jobs:
  unittests:
    name: unit_tests
    runs-on: macos-latest
    steps:
      - checkout: true
        submodules: true
      - name: 'Install dependencies'
        run: ./ci/mac_ci_setup.sh
      - name: 'Run tests'
        run: ./bazelw test //test/common/...
