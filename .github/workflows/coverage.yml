name: coverage

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - 'library/common/**'
      - 'test/**'

jobs:
  coverage:
    name: coverage
    runs-on: ubuntu-18.04
    timeout-minutes: 120
    container:
      image: envoyproxy/envoy-build-ubuntu:11efa5680d987fff33fde4af3cc5ece105015d04
      env:
        CC: /opt/llvm/bin/clang
        CXX: /opt/llvm/bin/clang++
        COVERAGE_THRESHOLD: 95
    steps:
      - uses: actions/checkout@v1
        with:
          submodules: true
      - name: 'Run coverage'
        continue-on-error: true
        run: |
          apt install -y dirmngr
          apt-key adv --keyserver keyserver.ubuntu.com --recv-keys DE19EB17684BA42D
          apt update
          apt install -y default-jdk
          "PATH=/opt/llvm/bin:${PATH} ./envoy/test/run_envoy_bazel_coverage.sh //test/..."
      - name: 'package coverage'
        run: |
          tar -czvf coverage.tar.gz generated/coverage
      - uses: actions/upload-artifact@v2
        with:
          name: coverage.tar.gz
          path: coverage.tar.gz
